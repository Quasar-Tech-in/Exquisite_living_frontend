// Ferris wheel card height, in px at scale 1. Shared between CardsFerrisWheel's
// own layout math and useViewportScale's height cap, which sizes the wheel so
// this card occupies a roughly constant fraction of viewport height — the two
// must never drift apart.
export const CARD_H = 240;
